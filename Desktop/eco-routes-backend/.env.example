# Environment Configuration

# Server
PORT=5000
NODE_ENV=development

# Database (PostgreSQL - update when ready)
# DATABASE_URL=postgresql://user:password@localhost:5432/ecoroutes

# JWT Authentication
# JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# API Configuration
API_PREFIX=/api/v1
